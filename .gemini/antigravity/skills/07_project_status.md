# Skill: Project Status & Roadmap

## Metadata
- **ID:** 07_project_status
- **Description:** Living record of the current build state, completed features, and high-priority roadmap items.
- **Scope:** Use this skill at the start of every session to understand "where we are" and at the end of every session to update "what we did."

---

## 1. Current Stage
- **Phase:** Pre-Launch / Release Candidate
- **Status:** 100% of core features, advanced features (AI, Automation), and marketing tools completed. Project is in final polishing phase.

## 2. What's Built (✅)

### Novena Hub (Backend & Platform)
- **Multi-Tenancy & Security:** Pegasus-based teams, hardened RBAC (5 roles), and Shareable Public Links with Expiring Tokens.
- **Zero-Touch Onboarding:** 4-step wizard, hardware auto-detection, "Plug Claim Play" logic, and Gateway Discovery API.
- **Device Management:** Intelligent Port Grid, fuzzy matching for device templates, progressive disclosure of raw register maps, and live connection testing.
- **Optimized Telemetry Engine:** MQTT ingestion to TimescaleDB, enhanced by Redis buffering, Celery Beat task flushers, and WebSockets with plan-gated latency.
- **Automation & Alerts:** "Cloud PLC" automation engine, Alert Presets, threshold rules, and remote control write-back logic.
- **AI & Insights:** "AI Chat With Your Data" using litellm function calling.
- **Operations & Maintenance:** Reactive and Preventive Maintenance Ticketing Workflow.
- **UI & UX Polish:** Premium 'Dark Energy' Bento-style Auto-Dashboards, Fleet Command Center with geographical maps, and Unified Hub Navigation.
- **Marketing & Content:** Wagtail CMS integration, multi-page marketing site overhaul, and pre-launch product blueprint.

### Novena Gateway (Gateway Daemon)
- **Provisioning:** Initialization of Novena Gateway with Plug Claim Play feature support.
- **Offline Resilience:** SQLite offline buffering with throttled replay to handle intermittent connectivity.
- **Bidirectional RPC:** Multithreaded RPC handler for real-time live reads and remote commands.
- **Network Stability:** Multi-WAN network watchdog implementation.
- **Firmware Management:** Zero-Downtime Blue/Green OTA Firmware Update mechanism.

## 3. High-Priority Roadmap (❌)
*Items are in order of execution priority.*

1. **Launch Preparation:** Comprehensive load testing, security auditing, and final bug bashes.
2. **Billing & Tiers:** Finalize Stripe integration to enforce strict subscription limits and plan-gated features (e.g., websocket latency limits).
3. **User Documentation:** Complete the public-facing knowledge base and API documentation for end-users.
4. **Mobile Optimization:** Continued polish for "Mobile Pass" to ensure all advanced dashboards are fully responsive for tablet/mobile.

## 4. Current Sprint Goal
**Focus:** Final launch preparations, bug fixing, and platform stabilization across both cloud and edge environments.

## 5. Recent Activity Log
- **2026-07-02 (Novena Platform Rename):** Renamed the product family to Novena Platform across the cloud and gateway codebases. The cloud Django project package is now `novena_hub` (Novena Hub), the edge gateway package/service is now `novena_gateway` / `novena-gateway` (Novena Gateway), active branding/config/docs/templates now use Novena naming, and domain examples use environment placeholders instead of hardcoded Novena domains. Added `homepage model rename migration` and repaired the Wagtail homepage data migration so fresh database replay succeeds after the model rename. Created local PostgreSQL database `novena_hub` for verification and added `~/.venvs/novena` as a symlink to the existing environment. Verified `python manage.py check`, `python manage.py makemigrations --check --dry-run`, full `python manage.py migrate --noinput` on a fresh `novena_hub` database, `python manage.py migrate --check --noinput`, focused Django tests for chat/devices/telemetry, `npm run type-check -- --pretty false`, and `python3 -m compileall -q novena_gateway tests`. Edge runtime/unit tests remain dependency-blocked in WSL until gateway dependencies such as `sdnotify`, `simplejson`, and `pymodbus` are installed.
- **2026-06-27 (Gateway Claim + Assisted Commissioning):** Implemented the customer-ready gateway provisioning/commissioning split. Added `GatewayInventory` as the factory registry and `Gateway.lifecycle_status` (`claimed`, `online`, `commissioning`, `active`) so Cloud can verify manufactured serial numbers before account binding. Moved claim logic into `apps.devices.services.claim_gateway_for_team`, wired onboarding and gateway creation through it, and kept MQTT credentials based on serial number + printed claim code for v1. Onboarding now triggers Edge discovery after the gateway is online, then generates/pushes connector config after selected devices/templates are confirmed. Fixed Cloud-generated Modbus config to match Edge's top-level `type/name/config` connector contract and normalize template types like `float32` to Edge types like `32float`. Updated Edge factory `config.json` to ship with Cloud identity/connectivity, discovery/remote config/RPC/heartbeat enabled, and empty `connectors: []`; updated Edge remote config handling so `connector_update` can replace the connector list while preserving gateway identity/MQTT settings. Verified with `python manage.py test apps.devices.tests`, `python manage.py check`, `python manage.py migrate --check --noinput`, `python manage.py makemigrations --check --dry-run`, Edge `python3 -m unittest tests.test_remote_config -q`, and JSON validation of Edge `config.json`.
- **2026-06-27 (WSL Post-Migration Health Check):** Re-verified the WSL-native Novena Hub stack at `/home/shouheng/projects/Novena-Hub`. Confirmed Django dev server is listening on `0.0.0.0:8000` and returns HTTP 200 on `127.0.0.1:8000`; Vite is listening on `0.0.0.0:5173` and returns HTTP 200 for `/static/@vite/client`; Redis responds to `PING`; PostgreSQL 17 is active and accepting connections on `127.0.0.1:5432`; Celery reports one online worker and Beat is successfully running telemetry/log flush tasks; Mosquitto is listening on `0.0.0.0:1883`, the MQTT consumer is connected, and a local publish/subscribe smoke test returned `mqtt-ok`. `python manage.py check` completed with existing non-blocking warnings, `python manage.py showmigrations --plan` shows migrations applied, and `npm run type-check -- --pretty false` passed. Also searched Cloud/Edge repos for stale `/mnt/d` and `Novena Project` path references with no matches. Novena Gateway local runtime is not yet provisioned in WSL: `python3 -m novena_gateway.main --help` fails because `sdnotify` is missing, and `python3 -m pytest` fails because `pytest` is not installed.
- **2026-06-26 (WSL Node Upgrade):** Upgraded WSL Node.js from Ubuntu package `v18.19.1` to NodeSource Node.js `v20.20.2` with npm `10.8.2` to satisfy the Tailwind/Vite engine requirement. Re-ran `npm install`, verified `npm run type-check -- --pretty false`, restarted the native dev stack, and confirmed Vite client `http://127.0.0.1:5173/static/@vite/client` returns HTTP 200 while Django remains HTTP 200 on `127.0.0.1:8000`.
- **2026-06-26 (WSL-Native Project Migration):** Migrated the active Novena Hub working copy from the Windows-mounted path `/mnt/d/Novena-Hub` to WSL-native storage at `/home/shouheng/projects/Novena-Hub`. Kept the D-drive copy intact as a fallback. Reused the WSL-native Python environment at `~/.venvs/novena`, synced Python dependencies with `uv sync --frozen --active`, installed frontend dependencies with `npm install`, and updated local WSL instructions plus `scripts/start_wsl_dev_stack.sh` to use the native path. Verified `python manage.py check`, `python manage.py migrate --check --noinput`, `npm run type-check -- --pretty false`, Django HTTP 200 on `127.0.0.1:8000`, Vite on `127.0.0.1:5173`, Mosquitto MQTT smoke publish/subscribe on `127.0.0.1:1883`, Celery Beat/worker connected to Redis, and PostgreSQL 17 + TimescaleDB running locally in WSL. Remaining external hardware item: expose MQTT to the Pi on Laptop 1 LAN IP `192.168.100.7:1883` via Administrator portproxy or a Windows LAN-bound Mosquitto listener.
- **2026-06-26 (WSL Development Default):** Switched local development guidance from Git Bash/Windows shell toward WSL. Added repo-level instruction in `AGENTS.md` to default to `/mnt/d/Novena-Hub` with `source .venv-wsl/bin/activate`. Cleaned stale virtualenv folders, keeping `Novena-Hub\.venv-wsl` as the new primary WSL environment and `Novena-Hub\.venv` only as a temporary Windows fallback until WSL is verified end-to-end with Django, Vite, Redis, Celery, Mosquitto, MQTT ingestion, and Pi CM4 connectivity.
- **2026-06-26 (Local Dev + MQTT Hardware Test Notes):** Documented Windows Laptop 1 local development quirks in `docs/local_development_machine_notes.md`. Django starts cleanly but sandboxed `Start-Process` can fail due to duplicate `Path`/`PATH` environment keys; Vite can fail in sandbox with `Error: spawn EPERM` while spawning `esbuild`, so both may need to be launched outside the sandbox/elevated shell. Confirmed current hardware test uses plain MQTT on `1883` from Pi CM4 to Laptop 1 (`192.168.100.7:1883`) with no TLS; production target remains MQTT over TLS on `8883`. Clarified that `1884` is the current Mosquitto dynamic-security admin/provisioning listener, not the telemetry listener, and should remain private/restricted if used.
- **2026-06-25 (MQTT Test Prep):** Checked local MQTT broker readiness for Raspberry Pi CM4 gateway testing. Repo includes Mosquitto configuration under `mosquitto/` and Docker Compose exposes `1883`, `1884`, and `9001`, with Django configured for `MQTT_BROKER_HOST=localhost` and `MQTT_BROKER_PORT=1883`. No MQTT broker is currently listening locally on `127.0.0.1:1883`; Mosquitto CLI/service is not installed on Windows, Docker CLI is unavailable, and the Python fallback broker dependency `amqtt` is not installed.
- **2026-06-25:** Started local development servers for Novena Hub. Confirmed Django dev server is running at `http://127.0.0.1:8000/` with HTTP 200, and Vite dev server is running at `http://127.0.0.1:5173/static/` with the Vite client endpoint returning HTTP 200. Vite required an elevated launch because sandboxed execution hit Windows `EPERM` while spawning esbuild; relaunched with `npm.cmd run dev -- --host 127.0.0.1 --force`.
- **2026-06-08:** Executed Phase 2. Created standard register maps template fixtures. Built Auto-Dashboard Widget engine. Implemented Command Center map mockup. Created Zero-Downtime Blue/Green OTA Update mechanism across cloud and edge.
- **2026-06-10:** Completed Sprint optimizations: Wagtail CMS marketing site, AI Chat with Data, shareable links, and ticketing workflows. Edge offline buffering and multi-WAN watchdog deployed.
- **2026-06-12:** Completed Device Template Onboarding feature. Implemented progressive disclosure for raw register maps, and a Live Connection Test module directly within the quick-add modal. Fixed legacy view test mocks and registered device signals.
- **2026-06-12 (Later):** Conducted a holistic codebase review of Novena Hub and Novena Gateway. Updated project status to reflect massive progress across all 5 sprints and 4 phases, graduating the project status to Pre-Launch.
- **2026-06-13:** Executed pre-launch hardening implementation plan (Phases 1-4). Hardened edge security (daemon runs as unprivileged 'novena' user), locked down production settings (HSTS, ALLOWED_HOSTS, Mailgun), updated Stripe pricing metadata & UI with direct checkout and offline simulation, and enforced dynamic application-level data retention (7, 30, 90-day limits) on telemetry API, dashboard views, and CSV exports. Created test suites for alerts and telemetry with 100% pass rate.
- **2026-06-16:** Finalized SaaS pricing strategy to use "Feature-Gating" Tripwires. Updated `06_business_context.md` with new Starter ($99), Business ($399), and Enterprise ($699) tiers to naturally funnel SME clients. Generated comprehensive 5-year Investor Analysis and TAM projection against Carro.
- **2026-06-16 (Later):** Set up and synced real Stripe products (Starter, Business, Enterprise) to local `.env` and `metadata.py`. Revamped the UI/UX of the subscription upgrade page with premium dark-mode aesthetics, glowing gradients, and unconstrained max-width containers. Tested local webhook forwarding using Stripe CLI.
- **2026-06-16 (Even Later):** Fixed HTMX interception bug with the "Manage billing" button pointing to Stripe Customer Portal. Improved the "Active Subscription" UI by exposing the user's default payment method card details and adding interactive visual progress bars for Gateway and Device usage limits based on their active Stripe Product.
- **2026-06-18:** Upgraded alert notifications engine to use Amazon SES via django-anymail and direct Meta WhatsApp Graph API. Implemented targeted notification dispatch (recipients selection via HTMX search dropdown and Alpine.js badges). Added view-level model validation to enforce selecting recipients when notifications are active. Refactored dispatches to run asynchronously as Celery tasks (`dispatch_alert_email_task` and `dispatch_alert_whatsapp_task`). Created full automated pytest suites verifying both targeted dispatches, Meta API requests mocking, and search HTMX endpoint permissions. Improved search matching to query the username field and added a "No members found" visual fallback message to the partial search results template. Fixed Django Debug Toolbar (DjDT) routing conflict where catch-all Wagtail URL patterns shadowed DjDT routes, causing 404 overlay errors during AJAX/HTMX searches.
- **2026-06-18 (Later):** Overhauled the Team application architecture. Moved team setup directly into the user signup flow (`TeamSignupForm`) requiring a company name upfront to eliminate auto-generated team names. Cleaned up global navigation by removing the legacy sidebar team switcher and consolidating Account, Team Settings, Billing, and Team Switching into the top-right user profile dropdown. Extended member management to expose the `CustomUser.phone_number` field so users can configure their WhatsApp numbers directly within the Team Settings view. Completely rewrote all team management templates (`manage_team.html`, `list_teams.html`, `team_membership_details.html`, `team_invitations.html`) discarding legacy Pegasus CSS in favor of premium Tailwind/DaisyUI components. Fixed several edge cases including `IntegrityError` crashes on duplicate invitation acceptance, `Team.email` property crashing when no admin is present, and API inconsistencies in default role assignment (`ROLE_OWNER` instead of `admin`).
- **2026-06-18 (Later - Profile & Custom Fields Polish):** Redesigned the user profile dashboard (`profile.html` and its components) using modern Tailwind CSS and DaisyUI, replacing legacy Bulma-based Pegasus styling. Implemented an Alpine.js-powered settings sidebar selector that persists the active tab across page reloads using `localStorage`. Added `job_title` and `department` fields to the `CustomUser` model to capture professional roles within the enterprise IoT team structure, generated and executed the database migrations, and exposed these fields on the profile settings forms. Redesigned the Two-Factor Authentication, Linked Accounts, and API Credentials modules with responsive components and secure confirmation alerts.
- **2026-06-18 (Later - Dropdown Navigation Upgrade):** Upgraded the global user profile navigation dropdown (`top_nav.html`) to include a personalized user identity header card (with avatar, display name, and email), active team role badge context (e.g. Owner/Admin/Viewer), and quick links to core resources (Documentation, Help Center/FAQ) to improve context awareness when navigating settings inside the application.
- **2026-06-18 (Even Later - Contact Support Modal):** Added a global "Contact Support" ticket dispatch system. Built an HTMX-powered DaisyUI modal (`contact-support-modal` in `app_base.html`) allowing users to compose technical, billing, or operational tickets. Created the `contact_support` view in the `support` app (`apps/support/views.py`) that handles category selection, validates inputs, and automatically dispatches support emails to the configured support address (`PROJECT_METADATA["CONTACT_EMAIL"]`). Built the success feedback template (`contact_success.html`) displaying confirmation checks and operational SLA expectations.
- **2026-06-18 (Alert System Hardening):** Hardened the alert and notification engine. Implemented cooldown logic hardening to include `"active"` and `"acknowledged"` states, preventing spam. Added duration-based triggers with database checks over TimescaleDB telemetry historical windows (and preceding points fallback). Added automatic resolution logic to mark alerts resolved and send green-styled email/WhatsApp notifications. Integrated asynchronous webhooks through a Celery task (`dispatch_alert_webhook_task`). Wrote a comprehensive unit test suite in `apps/alerts/tests.py` covering all seven improvements, with 100% test pass rates.
- **2026-06-19:** Restarted local Django and Vite servers. Built the Configurable Alert Escalation to Maintenance Tickets, allowing rules to auto-create tickets and bind standard checklist templates. Implemented the "Zero-App" WhatsApp Maintenance Loop, letting technicians receive WhatsApp/Email notifications on assignment, check off tasks with `DONE <index>`, change ticket status with `STATUS <status>`, and log comments. Created validation rules requiring at least one notification channel, set up a global webhook endpoint, and wrote 7 passing pytest unit tests.
- **2026-06-22:** Conducted comprehensive CTO-level progress review and business analysis. Performed deep-dive audits of both Novena Hub (settings, apps, Docker, tests) and Novena Gateway (connectors, install scripts, config, tests). Generated platform maturity scorecard across 12 dimensions. Assessed field testing readiness (85%) and production beta readiness (65%). Identified critical gaps: no production Dockerfile, thin test coverage (5/18 apps), inconsistent database architecture (plain Postgres vs TimescaleDB), missing CI/CD pipeline, and branding inconsistencies ("iiot" vs "Novena"). Documented prioritized roadmap for field testing (this week), beta launch (2 weeks), and production (2 months). Created `cto_progress_review.md` artifact with full analysis.
- **2026-06-20:** Implemented the Maintenance Compliance & SLA Shareable Link workflow. Added file attachment and guest author support to `TicketComment`. Configured automatic ticket assignment and notification properties on `PreventiveSchedule`. Built the `SharedTicketLink` model using expiring UUID tokens, and implemented the guest contractor portal for checklist toggles via HTMX, photo uploads, and status resolutions with incomplete task validation. Added the link configuration panel to the internal ticket detail view and verified all 12 test assertions with 100% pass rates. Fixed a NameError 500 bug on form saves, converted 'Next Due Date' to use a simpler date-only picker, and polished the Auto-Assignment and error presentation on PM Schedule setup. Built the `ScheduleUpdateView` and registered `/schedules/<int:pk>/edit/` to make Preventive Schedules interactive and editable, updated the schedule listing table, and verified all 13 test assertions with 100% pass rates. Refactored the preventive ticket generation code (`create_pm_ticket`) to `services.py` to adhere to Rule 5 (Service Layer Pattern) and defined `advance_schedule_due_date` there. Added the `trigger_preventive_schedule` view and `/schedules/<int:pk>/trigger/` URL route for manual ticket generation. Updated `schedule_list.html` to display all generated tickets under the schedule name as clickable badge links and added a `Generate Ticket` action button. Wrote unit tests in `tests.py` verifying the trigger endpoint, permissions, and due date advancement, achieving a 100% pass rate (15 tests OK). Conducted a thorough code and UI/UX review of the alerts and maintenance apps. Added dynamic `ticket` and `alert` properties to `Alert` and `MaintenanceTicket` models to support decoupled bidirectional links. Refactored `auto_create_ticket` to accept a `force` parameter, allowing manual escalation of active/acknowledged alerts to reactive tickets. Implemented `escalate_alert_to_ticket` view and `/alerts/<alert_id>/escalate/` endpoint. Updated `alert_row.html` to display ticket status badges and an `Escalate` trigger button, and modified `ticket_detail.html` to display a clickable, direct link to the triggering alert. Restructured the alerts pages into a unified tabbed Alerts Center dashboard (`Triggered Alerts` vs `Monitoring Rules`), removing redundant rule listings from the alerts page. Wrote unit tests for alert escalation and verified all 25 tests pass successfully.
- **2026-06-23:** Implemented configuration validation, sanitization, and periodic discovery background thread logic on Novena Gateway (Phase 1). Refactored gateway configuration verification into a standalone, reusable static method `validate_config`. Exposed this logic in the gateway CLI via a new `--validate-only` (`-v`) argument, returning exit status 0 (valid) or 1 (invalid). Integrated this validator as a pre-flight installation check in `install.sh` to output clean warning blocks. Added inbound sanitization inside `RemoteConfigHandler` to validate new configurations before saving them to disk, preventing edge crashes and returning error ACKs to the cloud on failures. Added periodic background discovery scan daemon thread `DiscoveryPeriodicScan` inside `DiscoveryService` configured via `scan_interval_seconds` (defaults to 3600s). Verified all changes with manual CLI validations and executed the full test suite with 100% of the 54 unit tests passing successfully.





