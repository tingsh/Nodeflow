# Novena Project Status

## Current Stage
- Phase: Pre-Launch / Release Candidate.
- Status: Core platform features, advanced AI and automation features, gateway commissioning, and marketing content are largely built. Current work is final polish, reliability, billing enforcement, documentation, and production-like local stabilization.

## Current Sprint Goal
Final launch preparation, bug fixing, and platform stabilization across Novena Hub and Novena Gateway.

## What Is Built
- Novena Hub: multi-tenant Django SaaS, teams and RBAC, onboarding, devices, sites, gateways, telemetry ingestion, TimescaleDB storage, Redis and Celery buffering, WebSockets, dashboard widgets, alerts, automations, maintenance tickets, Wagtail marketing content, Stripe billing foundations, and AI insights.
- Novena Gateway: Python gateway daemon, Modbus connector support, remote config handling, RPC, attribute sync, payload formatting, offline buffering, discovery, logging, network watchdog, and service/install files.
- Local development: WSL-native Hub stack is the preferred path with PostgreSQL and TimescaleDB, Redis, Mosquitto, Django, Celery worker, Celery Beat, MQTT consumer, and Vite.

## High-Priority Roadmap
1. Launch preparation: load testing, security review, production deployment checks, and hardware bug bash.
2. Billing and tiers: finish Stripe enforcement for subscription limits and plan-gated latency/features.
3. User documentation: customer setup docs, gateway onboarding docs, and API/telemetry docs.
4. Mobile polish: ensure device pages, dashboards, and operational forms work cleanly on tablet/mobile.

## Recent Activity Log
- 2026-07-10 (SES Production Revisit Marker): Added a dedicated production revisit note to `docs/amazon_ses_setup.md` clarifying that SES production access, SNS event publishing, Anymail public webhook tracking, bounce/complaint tests, and CloudWatch reputation monitoring are intentionally deferred until Novena Hub is hosted on a public HTTPS domain. Local development testing remains focused on SES accepting password reset, invitation, and alert emails, with `EmailDelivery.status = sent` as the expected local audit endpoint.
- 2026-07-10 (SES Local Development Guide Simplification): Reworked `docs/amazon_ses_setup.md` around the current local-hosting goal: test SES sending for password reset, invitations, and alert emails without SES production access, SNS subscriptions, public Anymail webhooks, or event publishing. Updated local `.env` and `.env.example` so `AWS_SES_CONFIGURATION_SET_NAME` and `ANYMAIL_WEBHOOK_SECRET` are blank during local development, and adjusted `novena_hub/settings.py` so a blank SES configuration set is treated as disabled. Validation: `py_compile novena_hub/settings.py` passed; grep confirmed the guide frames SNS/event publishing/production access as skipped until public hosting.
- 2026-07-10 (SES Development Setup Adjustment): Updated `docs/amazon_ses_setup.md` for the current development flow by removing the SES production-access step, renumbering the guide, and switching example configuration names to development-safe resources (`novena-dev-email`, `novena-dev-ses-events`). Updated local `.env` to expose the SES backend settings and `ANYMAIL_WEBHOOK_SECRET`, replacing the old commented Mailgun example block. Validation: grep confirmed no production-access step remains and `.env` now contains the SES webhook secret variable.
- 2026-07-10 (SES Email Reliability And Audit): Reworked Novena Hub email delivery around Amazon SES only. Removed active Mailgun dependency/configuration, defaulted production email to Anymail SES, added SES region/configuration-set/webhook-secret settings, and documented AWS setup in `docs/amazon_ses_setup.md`. Added `EmailDelivery` audit tracking with provider message IDs, attempt counts, statuses, contextual links for alerts/invitations/maintenance/user/team, and Anymail tracking signal handling for delivered/bounced/complained/rejected/failed/deferred events. Alert emails now send through tracked per-recipient delivery with Celery retries and partial retry skipping; no-recipient alert rules create audit rows instead of disappearing silently. Team invitation emails now queue after transaction commit through Celery so web requests are not blocked by SES. Alert rule forms now require recipients when email/WhatsApp notification is enabled, and onboarding-created alert rules add the current user as the first recipient. Support, sales inquiry, and maintenance assignment/resolution emails were moved onto tracked delivery. Validation: Ruff passed on touched Python files; py_compile passed; `manage.py check` passed with existing dj-stripe/allauth/Gemini warnings; `makemigrations --check --dry-run` passed; focused pytest for alerts/events/invitation email passed (17 tests).
- 2026-07-10 (WhatsApp Alert Template Parameter Wiring): Added Novena-side support for parameterized WhatsApp alert templates. `dispatch_alert_whatsapp_task` now sends seven body parameters for custom templates: status, rule name, device name, trigger value, severity, trigger timestamp, and alert dashboard URL. Documented the expected Meta template `novena_alert_notification` in `docs/whatsapp_integration_setup.md`; `hello_world` remains the default until the custom template is created/approved in Meta and `WHATSAPP_ALERT_TEMPLATE_NAME` is changed. Validation: compileall passed for alerts/settings; Ruff passed on touched WhatsApp files; focused WhatsApp dispatch pytest passed.
- 2026-07-10 (WhatsApp Alert Template Delivery Fix): Diagnosed Meta WhatsApp delivery after outbound text messages returned HTTP 200 but did not arrive on the recipient phone. Confirmed the configured system-user token `novena_whatsapp_api` has `whatsapp_business_management` and `whatsapp_business_messaging`, and confirmed the sender is the Meta Cloud API test number `+1 555-198-5766`. Verified Meta's built-in `hello_world` template delivered to `+6591053953`, while business-initiated free-form text did not. Updated alert WhatsApp dispatch to send a configurable template (`WHATSAPP_ALERT_TEMPLATE_NAME`, default `hello_world`; `WHATSAPP_ALERT_TEMPLATE_LANGUAGE`, default `en_US`) and to log Meta response bodies with `wamid`/`message_status`; generic maintenance/reply WhatsApp sends still use free-form text. Restarted Django/Celery and verified an alert-triggered template message was accepted by Meta, then cleaned up the temporary template smoke-test rule/alert.
- 2026-07-10 (WhatsApp Alert Live Delivery Test): Ran the outbound WhatsApp alert validation against the Meta Cloud API using `pilot.audit@novena.local` with `+6591053953` on the `pilot-cold-room` team. Pre-flight confirmed Django, Celery, `WHATSAPP_PROVIDER=meta`, Graph API `v21.0`, and configured Meta phone ID/token. Direct Celery WhatsApp smoke message succeeded, service-level alert trigger and resolution messages both succeeded, and MQTT telemetry through `v1/gateway/telemetry` created a telemetry row plus alert and dispatched WhatsApp successfully. Cleaned up the temporary `WhatsApp Smoke Test - 2026-07-10` rule, two test alerts, and the `whatsapp_test_signal` telemetry row; kept the phone number on the pilot audit user for future tests.
- 2026-07-09 (Public Website Revamp): Reworked the Wagtail-backed public marketing site around Novena Platform as AI-powered industrial IoT for SME operations. Added reusable marketing StreamField blocks for metric bands, rendered product visuals, process steps, platform capabilities, vertical solution cards, proof strips, and public lead capture. Refreshed the homepage, Platform, Solutions, Pricing, and About page content through new Wagtail migrations; removed old fake-logo/wrong-brand/AI-generated-image references from rendered public pages; updated navigation and footer to real buyer paths; added a public sales inquiry endpoint and tests. After browser review, softened the public marketing palette away from pure white backgrounds and near-black text by adding scoped low-glare marketing surfaces plus softer nav/footer colors. Validation: applied migrations locally; `manage.py test apps.web.tests.test_basic_views apps.support.tests` passed (8 tests); `manage.py makemigrations --check --dry-run` passed; `npm run build` passed with the existing HTMX eval warning; rendered checks on a fresh local server confirmed the five public pages return HTTP 200 with updated titles and no old generated-image filenames/wrong-brand terms. Playwright screenshot capture was attempted with a temporary npm cache but blocked by missing system browser dependency `libnspr4.so`.
- 2026-07-09 (Round 2 Hardware Replay Prep): Prepared the second Laptop 2 -> Pi CM4 -> Laptop 1 hardware replay setup. Updated the Laptop 2 Modbus simulator to support scenario-specific factory/cold/facilities register maps and current pymodbus import names. Added a Round 2 hardware replay runbook at `docs/hardware_replay_runbook_2026-07-09.md` using the original local hardware-test MQTT path on Laptop 1 port `1883`. Ran `pilot_readiness_audit prepare`, which printed hardware replay serials/claims: `NOV-AUDIT-COLD-HW`/`4C9DFAA0`, `NOV-AUDIT-FACTORY-HW`/`F157DFD4`, and `NOV-AUDIT-FACILITY-HW`/`695E82D6`. Gateway-side prep included explicit discovery `tcp_hosts` support so the Pi can scan Laptop 2 at `10.0.0.20:502` even when its default route is Wi-Fi, field-test CM4 configs under `install/field-test-configs/`, and a Gateway release archive at `/home/shouheng/projects/Novena-Gateway/dist/novena-gateway-cm4-field-test-2026-07-09.tar.gz`. Also pinned Gateway `pymodbus==3.8.0` for Pi compatibility. Validation: Hub migrations applied; simulator/discovery syntax passed; field config JSON passed; archive contents verified.
- 2026-07-09 (WhatsApp Cloud API Configuration Prep): Audited the existing alerts and maintenance WhatsApp flow, confirmed outbound alert/ticket dispatch and inbound maintenance commands were already wired, and finished the configuration layer. Centralized Meta WhatsApp text sending in `apps/alerts/whatsapp.py`, made `WHATSAPP_GRAPH_API_VERSION` configurable, removed stale Twilio settings, added explicit `WHATSAPP_VERIFY_TOKEN`, updated `.env.example`, and documented Meta/WhatsApp Business setup in `docs/whatsapp_integration_setup.md`. Noted that current outbound sends use Cloud API text messages, so production business-initiated alerts may require approved WhatsApp templates after Meta setup. Validation: `manage.py test apps.alerts apps.maintenance` passed (15 Django-discovered tests); `DJANGO_SETTINGS_MODULE=novena_hub.settings pytest apps/alerts/tests.py apps/maintenance/tests.py` passed (26 tests); WhatsApp-focused pytest rerun passed (2 tests); Ruff passed on touched Python files; `manage.py check` and compileall passed with existing dj-stripe/allauth/Gemini warnings.
- 2026-07-08 (Unified Device Command Audit): Unified customer-facing device commands so Quick Controls, On-Demand Read, Advanced Read/Write, and quick-add connection tests all create `DeviceCommand` audit records linked to the underlying `RpcCommand` transport record. Added `DeviceCommand.command_type`, nullable `value` for reads, and nullable `rpc_command` linkage; updated MQTT response handling and RPC timeout handling to mirror transport results into the customer audit trail. Updated device page JavaScript to post through the unified command endpoint and poll `DeviceCommand` status. Validation: `makemigrations --check --dry-run` passed; compileall for devices/telemetry passed; focused command/telemetry tests passed (12 tests); full device tests passed (60 tests); automations tests passed (4 tests); `manage.py check`, `npm run type-check`, and `npm run build` passed with existing warnings.
- 2026-07-08 (Billing Plan Metadata Alignment): Aligned subscription enforcement around the three active plans: Starter, Business, and Enterprise. `ProductMetadata` is now the single source of truth for gateway limits, device limits, telemetry retention days, and telemetry update interval. Removed duplicated plan-limit maps from `apps/subscriptions/enforcement.py`, routed telemetry retention helpers through enforcement, updated public pricing copy away from Professional, and enforced gateway limits during gateway claim. Added focused subscription enforcement regression tests. Validation: `manage.py test apps.subscriptions.tests` passed (14 tests); `manage.py test apps.subscriptions.tests.test_enforcement apps.devices.tests.test_gateway_commissioning` passed (20 tests); `manage.py check` passed with existing dj-stripe/allauth/Gemini warnings; `npm run type-check` passed; `npm run build` passed with existing HTMX eval warning.
- 2026-07-06 (Pilot Readiness Customer Simulation): Added a repeatable pilot-readiness audit harness for Novena Hub. The new `pilot_readiness_audit` management command prepares three customer personas/tenants, prints hardware replay serials and claim codes, generates simulation data for gateway discovery, live telemetry, alerts, maintenance tickets, preventive schedules, and contractor links, and provides snapshot counts. Added customer audit runbook and scorecard docs so simulation and physical hardware replay use the same checklist.
- 2026-07-05 (Gateway Self-Serve Reclaim + Pilot Readiness): Implemented the first customer-pilot hardening pass across Hub and Gateway. Hub now tracks gateway MQTT provisioning/credential rotation/bootstrap state, stores operational MQTT credentials hashed instead of using the sticker claim code as the day-to-day password, supports customer self-serve gateway release/reclaim without reburn, blocks required provisioning failures when enabled, adds discovery_hints and ranked discovery matching, and includes OTA checksum metadata. Gateway now supports bootstrap MQTT fallback/activation, deployment-mode placeholder validation, safer SQLite replay that preserves partial-failure batches, buffer health heartbeat attributes, checksum-enforced OTA downloads, and network_preflight RPC diagnostics. Validation: focused Hub gateway/telemetry tests passed (39 tests); Hub makemigrations check passed with no changes; Hub manage.py check passed with existing warnings; focused Gateway unit tests passed (44 tests); Gateway compileall passed.
- 2026-07-04 (Project Local Dev Skill): Added the repo-local Codex skill .agents/skills/novena-local-dev to make WSL development startup repeatable for Novena Hub. The skill documents the known-good WSL path, avoids PowerShell quoting pitfalls, wraps PostgreSQL/Redis readiness, Django checks, migration checks, Timescale verification, and delegates Mosquitto/Django/Celery/MQTT consumer/Vite startup to the existing scripts/start_wsl_dev_stack.sh. Validation: skill frontmatter passed quick_validate.py from WSL; bundled shell scripts passed bash -n.
- 2026-07-03 (Commissioning + Adaptive Dashboard Polish): Implemented a normalized commissioning context, checklist-driven onboarding states, discovery candidate buckets, config/first-telemetry completion routing, adaptive operations dashboard service, operations-led team dashboard, customer-safe freshness wording, and device-detail dashboard context polish. Added focused coverage for commissioning state, adaptive asset classification, widget hint ordering, freshness wording, onboarding/device context, and team-home dashboard routing. Validation: `manage.py check` passed with existing dj-stripe/allauth/Gemini warnings; focused Django tests passed for adaptive dashboard, commissioning context, gateway freshness, team home, onboarding HTMX device creation, and device-detail fallback interval.
- 2026-07-03 (CTO Progress Review): Reviewed Novena Hub and Novena Gateway progress after the verified local E2E hardware flow from Laptop 2 Modbus simulator through Raspberry Pi CM4 Novena Gateway, Mosquitto MQTT, and Novena Hub. Assessed product and business readiness, production deployment gaps, and current market and grant context. Validation: Django production deploy check completed with warnings for Stripe webhook validation, insecure default SECRET_KEY if not overridden, missing Stripe API keys in DB, and deprecated allauth settings; Gateway unittest discover ran 51 tests with 2 import errors due missing WSL packages (pymodbus, simplejson); focused Hub pytest command timed out after 60s.
- 2026-07-02 (Team Delete Typed Confirmation): Added a second-step typed team-name confirmation to the Owner-only delete team modal, matching the gateway deletion safety pattern. The first `I understand, permanently delete this team` click now advances to a confirmation form, and the delete endpoint rejects missing or mismatched `confirmation_team_name` values before deleting. Validation: `manage.py check` passed with existing warnings; focused team delete confirmation tests passed (3 passed); browser verification confirmed the Owner sees a required `confirmation_team_name` input and final Delete Team submit after the first confirmation step.
- 2026-07-02 (Team Delete Owner RBAC): Split destructive team deletion from general team management by adding an Owner-only `delete_team` RBAC permission, hiding the Danger Zone/Delete Team UI unless that permission is present, and protecting the delete endpoint with `require_permission("delete_team")`. Added focused regression coverage for Owner/Admin/member behavior. Validation: `manage.py check` passed with existing warnings; focused pytest cases for RBAC/delete-team UI and endpoint behavior passed (4 passed); restarted the WSL Django dev server because it runs with `--noreload`; browser verification on `/a/novena/team/` confirmed the Owner account sees exactly one Delete Team control.
- 2026-07-02 (Codex Skill Migration): Migrated active project guidance from Antigravity-style loose Markdown files to Codex repo skills under .agents/skills in both Novena Hub and Novena Gateway. Archived historical Antigravity skill files under docs/archive/antigravity_skills_2026-07-02/, refreshed AGENTS.md to point at Codex skills, and added focused skills for project status, Hub architecture, WSL development workflow, telemetry/gateway protocol, UI/product design, product strategy, and Gateway runtime.
- 2026-07-02 (Telemetry Freshness Stabilization): Updated gateway freshness display to compute gateway online/offline from last_seen heartbeat age instead of trusting stale stored status alone. Updated device detail fallback polling to use the team latency limit default of 10 seconds. Added tests for gateway freshness, stale gateway context, fallback interval, and Gateway device_id payload propagation. Pushed Hub commit 36b5acf and Gateway commit 6c095c7.
- 2026-07-02 (Signup Required Label): Updated TeamSignupForm.team_name to render Company / Team Name (required), verified browser rendering and native required behavior, and ran Django checks plus syntax compilation.
- 2026-07-02 (Signup Company Validation UX): Added browser-side required behavior for team signup name while keeping server-side blank validation. Focused signup tests timed out during test database setup before completion.
- 2026-07-02 (Wagtail Homepage Template Repair): Added templates/content/novena_home_page.html so the public homepage returns HTTP 200 instead of TemplateDoesNotExist.
- 2026-07-02 (Local Dev Stack Restart): Verified WSL-native local stack: PostgreSQL/TimescaleDB, Redis, Mosquitto, Django, Vite, Celery worker, Celery Beat, and MQTT consumer.
- 2026-07-02 (Novena Platform Rename): Renamed cloud package to novena_hub and edge package/service to novena_gateway / novena-gateway, refreshed branding/config/docs/templates, and verified migrations, Django checks, focused tests, frontend type-check, and gateway compile checks.
- 2026-06-27 (Gateway Claim + Assisted Commissioning): Added gateway inventory and lifecycle states, moved claim flow into service logic, generated Cloud-to-Edge Modbus config with deviceId, and updated Edge remote config behavior.
- 2026-06-27 (WSL Post-Migration Health Check): Confirmed Hub WSL services, Django, Vite, Redis, PostgreSQL, Celery, Mosquitto, MQTT consumer, and type-check were healthy. Gateway runtime still needed WSL dependency installation.
- 2026-06-26 (WSL-Native Project Migration): Moved active Hub working copy to /home/shouheng/projects/Novena-Hub, reused ~/.venvs/novena, installed dependencies, and documented the WSL local development path.
